import { rem, rgba } from 'csx/'

export const accentColor = rgba(255, 99, 71, 1)
export const fz = { sm: { fontSize: rem(0.8) }, xs: { fontSize: rem(0.7) } }
export const lhCopy = { lineHeight: 1.5 }
